---
// 📋 Lista de Pokémon - Grid responsivo optimizado
import type { PokemonDetails } from '../services/pokemon';
import PokemonCard from './PokemonCard.astro';

export interface Props {
  pokemons: PokemonDetails[];
  title?: string;
}

const { pokemons, title = "Pokédex" } = Astro.props;
---

<div class="w-full max-w-7xl mx-auto">
  <section class="space-y-8">
    <!-- Encabezado -->
    <div class="text-center space-y-4">
      <h1 class="text-4xl md:text-5xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-purple-400 via-pink-400 to-red-400">
        {title}
      </h1>
      <p class="text-slate-400 text-lg max-w-2xl mx-auto">
        Descubre el fascinante mundo de los Pokémon. Explora sus tipos, estadísticas y características únicas.
      </p>
      <div class="text-sm text-slate-500">
        Mostrando {pokemons.length} Pokémon
      </div>
    </div>

    <!-- Grid de Pokémon -->
    {pokemons.length > 0 ? (
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 justify-items-center">
        {pokemons.map((pokemon) => (
          <div class="w-full max-w-sm">
            <PokemonCard pokemon={pokemon} />
          </div>
        ))}
      </div>
    ) : (
      <div class="text-center py-16">
        <div class="text-6xl mb-4">🔍</div>
        <h3 class="text-xl font-semibold text-slate-300 mb-2">No se encontraron Pokémon</h3>
        <p class="text-slate-400">Intenta ajustar tu búsqueda o navegar por las páginas disponibles.</p>
      </div>
    )}
  </section>
</div>

<!-- Efectos visuales adicionales -->
<style>
  .grid {
    animation: fadeInUp 0.6s ease-out;
  }
  
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
