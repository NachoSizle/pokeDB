---
// 📋 Lista de Pokémon - Grid responsivo optimizado con defer loading
import type { PokemonData } from '../services/pokemonDB';
import PokemonCard from './PokemonCard.astro';

export interface Props {
  pokemons?: PokemonData[];
  showInitialLoad?: boolean;
}

let { pokemons = [], showInitialLoad = true } = Astro.props;

// 🚀 Para optimizar Core Web Vitals, solo cargamos los primeros 12 Pokémon inicialmente
if (showInitialLoad && pokemons.length === 0) {
  const { getAllPokemon } = await import('../services/pokemonDB');
  const allPokemons = await getAllPokemon();
  // Limitar a 12 para carga inicial más rápida
  pokemons = allPokemons.slice(0, 12);
}
---

<div class="w-full max-w-7xl mx-auto">
  <section class="space-y-8" aria-labelledby="pokemon-list-heading">
    <h2 id="pokemon-list-heading" class="sr-only">Lista completa de Pokémon disponibles</h2>
    
    <!-- Grid de Pokémon -->
    {pokemons.length > 0 ? (
      <div 
        class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 justify-items-center" 
        aria-label={`Lista con ${pokemons.length} Pokémon disponibles`}
      >
        {pokemons.map((pokemon) => (
          <div class="w-full max-w-sm">
            <PokemonCard pokemon={pokemon} />
          </div>
        ))}
      </div>
    ) : (
      <div class="text-center py-16" role="status" aria-live="polite">
        <div class="text-6xl mb-4" aria-hidden="true">🔍</div>
        <h3 class="text-xl font-semibold text-slate-300 mb-2">No se encontraron Pokémon</h3>
        <p class="text-slate-400">Intenta ajustar tu búsqueda o navegar por las páginas disponibles.</p>
      </div>
    )}
    
    <!-- Botón para cargar más Pokémon (mejora UX) -->
    {showInitialLoad && pokemons.length === 12 && (
      <div class="text-center mt-8">
        <button 
          id="load-more-btn"
          class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg font-medium transition-colors"
          aria-label="Cargar más Pokémon"
        >
          ⚡ Cargar más Pokémon
        </button>
      </div>
    )}
  </section>
</div>

<!-- Script para cargar más Pokémon progresivamente -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const loadMoreBtn = document.getElementById('load-more-btn') as HTMLButtonElement;
    if (loadMoreBtn) {
      loadMoreBtn.addEventListener('click', async () => {
        loadMoreBtn.textContent = '⏳ Cargando...';
        loadMoreBtn.disabled = true;
        
        try {
          // Cargar todos los Pokémon
          const response = await fetch('/api/search');
          const data = await response.json();
          
          if (data.success) {
            // Recargar página con todos los resultados
            window.location.href = '/?showAll=true';
          }
        } catch (error) {
          console.error('Error loading more Pokemon:', error);
          loadMoreBtn.textContent = '❌ Error - Intenta de nuevo';
          loadMoreBtn.disabled = false;
        }
      });
    }
  });
</script>

<!-- Efectos visuales adicionales -->
<style>
  .grid {
    animation: fadeInUp 0.6s ease-out;
  }
  
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
