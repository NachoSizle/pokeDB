---
// 📄 Página de detalle de Pokémon
// Generada estáticamente con una isla de interactividad para los favoritos.

export const prerender = true;

import { getAllPokemon } from '../../services/pokemonDB';
import MainLayout from '../../layouts/MainLayout.astro';
import { Image } from 'astro:assets';
import FavoriteButton from '../../components/FavoriteButton.jsx';

// 🚀 Genera todas las páginas de Pokémon en el momento del build
import { db } from 'astro:db';
import { Favorite } from '../../db/config';

// 1. 🗺️ Generar las rutas estáticas para cada Pokémon
export async function getStaticPaths() {
  const allPokemon = await getAllPokemon();
  
  // Optimización: Obtener todos los favoritos de una sola vez
  const favoriteIdsResult = await db.select({ id: Favorite.pokemonId }).from(Favorite).all();
  const favoriteIdSet = new Set(favoriteIdsResult.map(f => f.id));

  return allPokemon.map((pokemon) => {
    const isInitiallyFavorite = favoriteIdSet.has(pokemon.id);
    return {
      params: { id: pokemon.id.toString() },
      props: { pokemon, isInitiallyFavorite },
    };
  });
}

// props se reciben desde getStaticPaths
const { pokemon, isInitiallyFavorite } = Astro.props;

const pageTitle = `${pokemon.name} - PokeDB`;
const pageDescription = `Información detallada sobre ${pokemon.name}, incluyendo sus estadísticas, tipos y habilidades.`;

// 🎨 Función para obtener el color de fondo según el tipo de Pokémon
const getTypeColor = (type: string) => {
  const colors: { [key: string]: string } = {
    normal: 'bg-gray-400',
    fire: 'bg-red-500',
    water: 'bg-blue-500',
    electric: 'bg-yellow-400',
    grass: 'bg-green-500',
    ice: 'bg-cyan-300',
    fighting: 'bg-orange-700',
    poison: 'bg-purple-600',
    ground: 'bg-yellow-600',
    flying: 'bg-indigo-400',
    psychic: 'bg-pink-500',
    bug: 'bg-lime-500',
    rock: 'bg-yellow-800',
    ghost: 'bg-indigo-800',
    dragon: 'bg-indigo-600',
    dark: 'bg-gray-800',
    steel: 'bg-gray-500',
    fairy: 'bg-pink-300',
  };
  return colors[type] || 'bg-gray-400';
};
---

<MainLayout title={pageTitle} description={pageDescription}>
  <!-- 🔙 Botón volver -->
  <div class="pt-4 px-4">
    <div class="max-w-7xl mx-auto">
      <a 
        href="/"
        class="inline-flex items-center gap-2 px-4 py-2 bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-lg shadow-md hover:shadow-lg transition-all duration-300 text-gray-700 dark:text-gray-300 hover:text-blue-600 dark:hover:text-blue-400"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
        </svg>
        Volver a la Pokédex
      </a>
    </div>
  </div>

  <!-- 🎨 Contenedor principal -->
  <div class="max-w-4xl mx-auto p-4">
    <div class="bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-3xl shadow-2xl overflow-hidden">
      <!-- Cabecera -->
      <div class:list={['p-8 text-white text-center relative overflow-hidden', getTypeColor(pokemon.types[0])]}>
        <div class="absolute -top-10 -right-10 w-48 h-48 bg-white/20 rounded-full"></div>
        <div class="absolute -bottom-16 -left-10 w-40 h-40 bg-white/20 rounded-full"></div>
        
        <div class="relative flex justify-center items-center gap-4">
          <div>
            <h1 class="text-5xl font-bold capitalize tracking-wider mb-2" style="text-shadow: 2px 2px 4px rgba(0,0,0,0.3);">
              {pokemon.name}
            </h1>
            <p class="text-2xl font-semibold opacity-90">#{String(pokemon.id).padStart(3, '0')}</p>
          </div>
          <!-- ❤️ Botón de Favoritos (Isla Interactiva) -->
          <div class="absolute top-4 right-4">
            <FavoriteButton client:load pokemonId={pokemon.id} isInitiallyFavorite={isInitiallyFavorite} />
          </div>
        </div>
      </div>

      <!-- Contenido -->
      <div class="p-8 grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
        <!-- Imagen del Pokémon -->
        <div class="text-center">
          <Image 
            src={pokemon.sprite} 
            alt={`Sprite de ${pokemon.name}`} 
            width={256} 
            height={256}
            class="mx-auto drop-shadow-2xl transition-transform duration-500 hover:scale-110"
          />
        </div>

        <!-- Detalles -->
        <div class="space-y-6">
          <!-- Tipos -->
          <div>
            <h2 class="text-2xl font-bold text-gray-800 dark:text-white mb-3 border-b-2 border-gray-200 dark:border-gray-700 pb-2">Tipos</h2>
            <div class="flex gap-3">
              {pokemon.types.map((type) => (
                <span class={`px-4 py-1 rounded-full text-white font-semibold capitalize text-sm ${getTypeColor(type)}`}>
                  {type}
                </span>
              ))}
            </div>
          </div>

          <!-- Estadísticas -->
          <div>
            <h2 class="text-2xl font-bold text-gray-800 dark:text-white mb-4 border-b-2 border-gray-200 dark:border-gray-700 pb-2">Estadísticas</h2>
            <div class="space-y-3">
              {Object.entries(pokemon.stats).map(([stat, value]) => (
                <div>
                  <div class="flex justify-between mb-1">
                    <span class="text-base font-medium text-gray-700 dark:text-gray-300 capitalize">{stat.replace('-', ' ')}</span>
                    <span class="text-sm font-medium text-gray-700 dark:text-gray-300">{value}</span>
                  </div>
                  <div class="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2.5">
                    <div class="bg-gradient-to-r from-blue-500 to-purple-600 h-2.5 rounded-full" style={`width: ${Math.min((value / 200) * 100, 100)}%`}></div>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</MainLayout>
