---
import MainLayout from '../layouts/MainLayout.astro';
import PokemonList from '../components/PokemonList.astro';
import { getPokemons, getPokemonDetailsInParallel } from '../services/pokemon';

// 🚀 Obtenemos los primeros 20 Pokémon para óptimo rendimiento inicial
const pokemonListResponse = await getPokemons(20, 0);
const pokemonDetails = await getPokemonDetailsInParallel(pokemonListResponse.results);
---

<MainLayout 
  title="PokéDB - Tu Pokédex Digital Definitiva" 
  description="Explora el mundo Pokémon con nuestra Pokédex digital. Descubre tipos, estadísticas y características de todos los Pokémon."
>
  <PokemonList pokemons={pokemonDetails} />
</MainLayout>